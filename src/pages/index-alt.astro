---
import Layout from '~/layouts/PageLayout.astro';

const metadata = {
  title: 'Lawrence Piano Studio — Alternate',
  ignoreTitleTemplate: true,
};
---

<Layout metadata={metadata}>
  <!-- Alternate home page content coming soon -->
</Layout>
